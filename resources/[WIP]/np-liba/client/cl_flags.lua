function SetPedFlag(object, flag, enabled)
    exports['drp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['drp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['drp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['drp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['drp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['drp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['drp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['drp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['drp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['drp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['drp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['drp-flags']:SetObjectFlags(object, flag)
end